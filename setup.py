from setuptools import setup, find_packages

setup(
    name="",
    version="0.1.0",
    description="Python package for the code accompanying do-not-distribute-proj",
    author="",
    packages=find_packages(include=["src", "src.*"]),
)
